---
active: true
iteration: 1
max_iterations: 60
completion_promise: "DFIR_ENHANCEMENTS_COMPLETE"
started_at: "2026-04-09T21:07:51Z"
---

You are implementing DFIR enhancements for the memory-forensic Rust project at /Users/4n6h4x0r/src/memory-forensic following STRICT TDD. Read docs/plans/2026-04-10-dfir-enhancements-design.md for full context. STATE FILE: Read and update /Users/4n6h4x0r/src/memory-forensic/.ralph-progress.json each iteration (create if missing): {phase:1,completed:[],in_progress:null,notes:''}. IMPLEMENTATION ORDER (one walker per iteration, RED then GREEN): Phase1-Linux: io_uring, proc_hiding, user_ns_escalation, container_breakout, vdso_tamper, audit_events, shm_anomaly, netlink_forensics. Phase1-Windows: apc_injection, clr_heap, dkom_detect, tls_callbacks, section_objects. Phase2-Linux: fuse_abuse, timerfd_anomaly, landlock_bypass, msr_forensics, udev_forensics. Phase2-Windows: fiber_fls, job_objects, cfg_bypass, wow64_anomaly, heap_spray, wsl_artifacts. Phase3: remaining walkers from design doc. Final: memf-correlate and memf-export crate skeletons. PER-WALKER TDD PROTOCOL: RED=create src/walker.rs with todo\!() stub + struct + 3 failing tests + add to lib.rs + run tests to confirm FAIL + commit 'test(walker): RED'. GREEN=implement detection + run tests to confirm PASS + run full crate tests + commit 'feat(walker): GREEN' + update .ralph-progress.json. RULES: NEVER write impl before tests. NEVER combine RED+GREEN commit. NEVER implement multiple walkers per iteration. Check .ralph-progress.json at start of every iteration. Use cargo test -p memf-linux or -p memf-windows (never run both crates tests concurrently). Follow patterns from existing walkers (study bpf.rs, callbacks.rs). Entry structs derive Debug+Clone+Serialize. COMPLETION: Output <promise>DFIR_ENHANCEMENTS_COMPLETE</promise> ONLY when: all 28 Phase1+2+3 walkers in completed[], cargo test -p memf-linux shows 0 failures, cargo test -p memf-windows shows 0 failures, memf-correlate and memf-export skeletons exist with 3 tests each.
